# Git 实战案例

## 目录
- [场景1：日常开发流程](#场景1日常开发流程)
- [场景2：功能分支开发](#场景2功能分支开发)
- [场景3：紧急修复](#场景3紧急修复)
- [场景4：多人协作](#场景4多人协作)
- [场景5：回滚操作](#场景5回滚操作)
- [场景6：代码审查](#场景6代码审查)
- [场景7：冲突解决](#场景7冲突解决)
- [场景8：版本发布](#场景8版本发布)

---

## 场景1：日常开发流程

### 背景描述

你正在开发一个新功能，需要进行日常的代码提交、查看历史、修改提交等操作。

### 步骤详解

#### 1. 开始一天的工作

```bash
# 查看当前状态
git status
On branch main
Your branch is up to date with 'origin/main'.

nothing to commit, working tree clean

# 拉取最新代码
git pull origin main
Already up to date.

# 创建工作分支
git checkout -b feature/user-dashboard
Switched to a new branch 'feature/user-dashboard'
```

#### 2. 编写代码并提交

```bash
# 编写代码...

# 查看修改
git status
Changes not staged for commit:
  modified:   src/components/Dashboard.js
  modified:   src/styles/dashboard.css

# 查看具体修改
git diff src/components/Dashboard.js

# 添加文件
git add src/components/Dashboard.js

# 添加所有文件
git add .

# 提交（良好的提交信息）
git commit -m "feat(dashboard): add user statistics cards

- Add total users card
- Add active users card
- Add growth rate card
Closes #234"
```

#### 3. 继续开发

```bash
# 更多开发...

# 查看提交历史
git log --oneline
a3f2c1d feat(dashboard): add user statistics cards
b8e7c5a feat(dashboard): create dashboard layout

# 查看最近3次提交
git log -3 --oneline

# 查看分支图
git log --graph --oneline --all
* a3f2c1d (HEAD -> feature/user-dashboard) feat(dashboard): add user statistics cards
* b8e7c5a feat(dashboard): create dashboard layout
* 1d2e3f4 (origin/main, main) fix(api): resolve user endpoint timeout
```

#### 4. 修改最后一次提交

```bash
# 发现漏掉了一个文件
git status
Untracked files:
  src/components/DashboardCard.js

# 添加并修改最后一次提交
git add src/components/DashboardCard.js
git commit --amend --no-edit

# 或者修改提交信息
git commit --amend -m "feat(dashboard): add user statistics cards and card component

- Add total users card
- Add active users card
- Add growth rate card
- Create reusable DashboardCard component
Closes #234"
```

#### 5. 推送到远程

```bash
# 推送分支
git push -u origin feature/user-dashboard
```

### 关键要点

- ✅ 使用功能分支隔离开发
- ✅ 频繁提交，保持提交信息清晰
- ✅ 使用 `--amend` 完善最后一次提交
- ✅ 推送前检查提交历史

---

## 场景2：功能分支开发

### 背景描述

开发一个完整的功能模块，需要多天时间，涉及多个文件。

### 步骤详解

#### 1. 创建功能分支

```bash
# 确保在 main 并拉取最新代码
git checkout main
git pull origin main

# 创建功能分支（命名规范）
git checkout -b feature/payment-integration

# 查看分支
git branch
* feature/payment-integration
  main
```

#### 2. 开发过程中多次提交

```bash
# 第一天：基础框架
git add src/payment/api.js
git commit -m "feat(payment): create payment API module"

# 第一天：UI 组件
git add src/payment/components/PaymentForm.js
git commit -m "feat(payment): add payment form component"

# 第二天：测试代码
git add src/payment/__tests__/api.test.js
git commit -m "test(payment): add API unit tests"

# 第二天：配置文件
git add config/payment.json
git commit -m "chore(payment): add payment configuration"
```

#### 3. 查看工作进度

```bash
# 查看分支状态
git status
On branch feature/payment-integration
nothing to commit, working tree clean

# 查看提交历史
git log --oneline
4d3e2c1 chore(payment): add payment configuration
5c4b3f2 test(payment): add API unit tests
6a5d4e3 feat(payment): add payment form component
7b6e5f4 feat(payment): create payment API module

# 查看 main 分支的差异
git diff main
```

#### 4. 同步最新主分支

```bash
# 获取最新代码
git fetch origin

# 变基到最新的 main
git rebase origin/main

# 如果有冲突，解决后
git add .
git rebase --continue

# 强制推送（因为 rebase）
git push origin feature/payment-integration --force-with-lease
```

#### 5. 完成功能

```bash
# 最后检查
git log --oneline origin/main..HEAD
4d3e2c1 chore(payment): add payment configuration
5c4b3f2 test(payment): add API unit tests
6a5d4e3 feat(payment): add payment form component
7b6e5f4 feat(payment): create payment API module

# 推送到远程
git push
```

#### 6. 创建 Pull Request

在 GitHub 上创建 PR，包含：

```markdown
## 变更说明
实现支付网关集成功能

## 变更内容
- 创建支付 API 模块
- 添加支付表单组件
- 添加单元测试
- 添加配置文件

## 测试步骤
1. 访问 /payment 页面
2. 填写支付信息
3. 提交表单
4. 验证支付成功

## 相关 Issue
Closes #456
```

### 关键要点

- ✅ 功能分支命名清晰
- ✅ 频繁提交，逻辑清晰
- ✅ 定期同步主分支
- ✅ 完整的测试和文档

---

## 场景3：紧急修复

### 背景描述

生产环境发现严重 Bug，需要立即修复。

### 步骤详解

#### 1. 确认问题和分支

```bash
# 查看当前状态
git status
On branch feature/new-feature
# 有未完成的工作...

# 保存当前工作
git stash save "WIP: new feature development"

# 切换到主分支
git checkout main
git pull origin main

# 查看最新标签
git tag
v1.2.0  ← 当前生产版本
```

#### 2. 创建 hotfix 分支

```bash
# 从 main 创建 hotfix 分支
git checkout -b hotfix/critical-security-fix

# 或者从标签创建（如果 main 已前进）
git checkout -b hotfix/critical-security-fix v1.2.0
```

#### 3. 修复问题

```bash
# 快速修复问题
vim src/auth/AuthManager.js

# 测试修复
npm test

# 提交修复
git add src/auth/AuthManager.js
git commit -m "fix(auth): resolve security vulnerability in authentication

- Sanitize user input before validation
- Add rate limiting for login attempts
- Update error handling

Critical fix for production.
Fixes #789"
```

#### 4. 完成修复流程

```bash
# 合并到 main
git checkout main
git merge hotfix/critical-security-fix

# 打标签
git tag -a v1.2.1 -m "Hotfix: Critical security fix"

# 推送到远程
git push origin main
git push origin v1.2.1

# 合并到 develop（如果使用 Git Flow）
git checkout develop
git merge hotfix/critical-security-fix
git push origin develop

# 删除 hotfix 分支
git branch -d hotfix/critical-security-fix
```

#### 5. 恢复之前的工作

```bash
# 切回功能分支
git checkout feature/new-feature

# 恢复工作
git stash pop

# 合并 hotfix（如果需要）
git merge main

# 继续开发
```

### 关键要点

- ✅ 使用 `stash` 保存未完成的工作
- ✅ hotfix 分支命名清晰
- ✅ 快速修复，立即部署
- ✅ 记录详细的修复说明
- ✅ 打标签追踪版本

---

## 场景4：多人协作

### 背景描述

团队多人同时开发不同功能，需要协调合并代码。

### 步骤详解

#### 1. 查看远程分支

```bash
# 查看所有分支
git branch -a
* feature/user-profile
  main
  remotes/origin/HEAD -> origin/main
  remotes/origin/feature/cart-module
  remotes/origin/feature/user-profile
  remotes/origin/main

# 同事正在开发 feature/cart-module
```

#### 2. 并行开发

```bash
# 你的工作
git checkout feature/user-profile
vim src/components/Profile.js
git add .
git commit -m "feat(profile): add user avatar upload"

# 推送
git push origin feature/user-profile
```

#### 3. 处理依赖变更

```bash
# 同事的 feature/cart-module 修改了共享文件
# 你需要同步这些变更

git fetch origin

# 查看差异
git log origin/main..HEAD
git log origin/feature/cart-module..HEAD

# 变基到最新的 main
git rebase origin/main

# 如果有冲突
vim src/utils/helpers.js  # 解决冲突
git add src/utils/helpers.js
git rebase --continue
```

#### 4. 协作者沟通

```bash
# 标记同事进行审查
# 在 PR 中：
@john-doe 请 review 这个 PR，我修改了 API 调用部分

# 或者通过 PR 评论
我已经更新了 API 接口，请确保你的 cart-module 兼容新接口
```

#### 5. 集成测试

```bash
# 创建集成分支
git checkout main
git pull origin main
git checkout -b integration/user-profile-and-cart

# 合并两个功能
git merge feature/user-profile
git merge feature/cart-module

# 运行集成测试
npm run test:integration

# 如果通过，合并到 main
git checkout main
git merge integration/user-profile-and-cart
```

### 关键要点

- ✅ 定期同步远程分支
- ✅ 及时沟通变更
- ✅ 使用 rebase 保持历史整洁
- ✅ 集成测试验证功能
- ✅ Code Review 确保质量

---

## 场景5：回滚操作

### 背景描述

发布后发现严重问题，需要回滚到之前的版本。

### 步骤详解

#### 1. 情况评估

```bash
# 查看提交历史
git log --oneline -10
e8f7d6a bad: broken feature (current)
a3b2c1d feat: new feature
9f8e7d6 fix: minor bug
5b4a3f2 chore: update deps
2c1b0a9 release: v1.2.0 (stable) ← 回滚目标

# 查看 v1.2.0 标签
git show v1.2.0
```

#### 2. 方法一：创建反向提交（推荐）

```bash
# 回退到稳定版本
git revert e8f7d6a
# 编辑器打开，修改提交信息

# 解决可能的冲突
git add .
git revert --continue

# 推送
git push origin main

# 这样保留了完整历史
git log --oneline
f9e8d7b revert: "bad: broken feature"
e8f7d6a bad: broken feature
a3b2c1d feat: new feature
```

#### 3. 方法二：硬重置（紧急情况）

```bash
# ⚠️ 危险操作，仅在紧急情况使用

# 确认团队知道你要重置
# 在团队群里通知："正在回滚 main 到 v1.2.0"

# 重置到稳定版本
git reset --hard v1.2.0

# 强制推送
git push origin main --force

# 通知团队
git push origin main --force-with-lease  # 更安全
```

#### 4. 方法三：创建回滚分支

```bash
# 从稳定版本创建分支
git checkout -b hotfix/rollback-to-v1.2.0 v1.2.0

# 推送为新的主分支
git push origin hotfix/rollback-to-v1.2.0

# 在 GitHub 上切换主分支
# Settings → Branches → Switch default branch
```

#### 5. 验证回滚

```bash
# 检查当前状态
git log --oneline -3
2c1b0a9 release: v1.2.0
5b4a3f2 chore: update deps
9f8e7d6 fix: minor bug

# 检查代码
git diff v1.2.0 HEAD
# 应该没有差异

# 部署验证
# 确认生产环境正常运行
```

### 关键要点

- ✅ 优先使用 `revert` 而非 `reset`
- ✅ 紧急情况也要与团队沟通
- ✅ 使用标签定位稳定版本
- ✅ 验证回滚是否成功
- ✅ 记录回滚原因

---

## 场景6：代码审查

### 背景描述

提交 Pull Request 后，收到审查反馈，需要修改代码。

### 步骤详解

#### 1. 创建 Pull Request

```bash
# 推送功能分支
git push -u origin feature/api-cache

# 在 GitHub 上创建 PR
# 标题：feat(api): add response caching for better performance
```

#### 2. 接收反馈

```markdown
# Reviewer @jane-smith 的评论：

在 src/cache/CacheManager.js:45 行：
建议使用 Map 而不是 Object，因为：
1. Map 的键可以是任意类型
2. Map 有内置的 size 属性
3. Map 在频繁增删时性能更好

在 tests/cache.test.js：
缺少缓存失效的测试用例
```

#### 3. 修改代码

```bash
# 切换到功能分支
git checkout feature/api-cache

# 修改 CacheManager.js
vim src/cache/CacheManager.js
# 将 Object 改为 Map

# 添加测试
vim tests/cache.test.js
# 添加缓存失效测试

# 提交修改
git add src/cache/CacheManager.js tests/cache.test.js
git commit -m "fix(cache): use Map instead of Object and add tests

- Switch from Object to Map for better performance
- Add cache invalidation test cases
- Address review feedback from @jane-smith"
```

#### 4. 推送更新

```bash
# 推送更新
git push origin feature/api-cache

# PR 自动更新，审查者可以看到新的变更
```

#### 5. 继续审查

```bash
# 又收到反馈：
# 请在 README 中添加缓存配置说明

# 再次修改
vim README.md
git add README.md
git commit -m "docs(cache): add configuration documentation"
git push
```

#### 6. 审查通过，合并

```bash
# PR 被批准并合并
# 在 GitHub 上点击 "Merge pull request"

# 删除本地分支
git checkout main
git pull origin main
git branch -d feature/api-cache

# 删除远程分支
git push origin --delete feature/api-cache
```

### 关键要点

- ✅ 清晰的 PR 描述
- ✅ 积极响应反馈
- ✅ 分次提交，逻辑清晰
- ✅ 引用审查者表示感谢
- ✅ 及时清理分支

---

## 场景7：冲突解决

### 背景描述

多人修改同一文件，产生冲突。

### 步骤详解

#### 1. 发现冲突

```bash
# 尝试合并分支
git merge feature/user-settings
Auto-merging src/components/User.js
CONFLICT (content): Merge conflict in src/components/User.js
Automatic merge failed; fix conflicts and then commit the result.

# 查看状态
git status
both modified:   src/components/User.js
```

#### 2. 查看冲突内容

```bash
# 查看冲突文件
cat src/components/User.js
<<<<<<< HEAD
const updateUser = (user) => {
  return fetch(`/api/users/${user.id}`, {
    method: 'PUT',
    body: JSON.stringify(user),
  });
};
=======
const updateUser = async (user) => {
  const response = await fetch(`/api/users/${user.id}`, {
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(user),
  });
  return response.json();
};
>>>>>>> feature/user-settings
```

#### 3. 分析冲突

```bash
# HEAD (当前分支)：同步版本
# feature/user-settings：异步版本 + headers

# 需要合并两者的优点
```

#### 4. 解决冲突

```bash
# 编辑文件
vim src/components/User.js

# 合并后的代码：
const updateUser = async (user) => {
  const response = await fetch(`/api/users/${user.id}`, {
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(user),
  });
  return response.json();
};

# 保存并退出
```

#### 5. 标记解决

```bash
# 添加解决后的文件
git add src/components/User.js

# 查看状态
git status
All conflicts fixed but you are still merging.

# 完成合并
git commit
```

#### 6. 使用合并工具（可选）

```bash
# 配置合并工具
git config --global merge.tool vimdiff
git config --global mergetool.vimdiff.cmd 'vimdiff "$BASE" "$LOCAL" "$REMOTE" "$MERGED"'

# 使用工具解决
git mergetool

# 退出工具后
git add src/components/User.js
git commit
```

### 关键要点

- ✅ 理解两边的修改意图
- ✅ 与协作者沟通
- ✅ 测试合并后的代码
- ✅ 不要直接删除冲突标记
- ✅ 记录冲突解决过程

---

## 场景8：版本发布

### 背景描述

准备发布新版本，遵循 Git Flow 流程。

### 步骤详解

#### 1. 创建 release 分支

```bash
# 确保在 develop 并同步最新代码
git checkout develop
git pull origin develop

# 创建 release 分支
git checkout -b release/2.0.0

# 更新版本号
vim package.json
# "version": "1.9.0" → "version": "2.0.0"

# 提交版本号变更
git add package.json
git commit -m "chore(release): bump version to 2.0.0"
```

#### 2. 发布前准备

```bash
# 更新 CHANGELOG
vim CHANGELOG.md

git add CHANGELOG.md
git commit -m "docs(release): update changelog for 2.0.0

## Features
- User authentication system
- Payment gateway integration
- Admin dashboard

## Fixes
- Fixed login timeout issue
- Resolved memory leak in cache

## Breaking Changes
- API endpoint /api/users changed to /api/v2/users"
```

#### 3. 测试 release

```bash
# 运行所有测试
npm test
npm run test:integration

# 构建生产版本
npm run build

# 本地测试生产构建
npm run serve:build
```

#### 4. 合并到 main

```bash
# 切换到 main
git checkout main
git pull origin main

# 合并 release 分支
git merge release/2.0.0

# 打标签
git tag -a v2.0.0 -m "Release version 2.0.0

Major release with new authentication and payment features.
See CHANGELOG.md for details."

# 推送
git push origin main
git push origin v2.0.0
```

#### 5. 合并回 develop

```bash
# 切换到 develop
git checkout develop

# 合并 release 分支
git merge release/2.0.0

# 推送
git push origin develop
```

#### 6. 部署

```bash
# CI/CD 自动触发部署
# 或手动部署：

# 部署到 staging
git checkout v2.0.0
npm run deploy:staging

# 验证 staging
# 测试主要功能...

# 部署到 production
npm run deploy:production
```

#### 7. 发布后

```bash
# 创建 GitHub Release
# 在 GitHub 网页上：
# - Releases → Draft a new release
# - Tag: v2.0.0
# - Title: Release v2.0.0
# - Description: Copy from CHANGELOG
# - Attach binaries

# 通知用户
# 发送邮件、公告等

# 清理 release 分支
git branch -d release/2.0.0
```

### 关键要点

- ✅ 版本号遵循语义化版本
- ✅ 完整的 CHANGELOG
- ✅ 充分测试
- ✅ 打标签标记版本
- ✅ 创建 GitHub Release
- ✅ 通知用户变更

---

## 综合练习

### 项目场景

创建一个简单的 Web 应用，涉及：
- 用户认证
- 数据展示
- 文件上传

### 练习步骤

#### 第1天：项目初始化

```bash
# 初始化项目
mkdir webapp
cd webapp
git init

# 创建初始文件
echo "# WebApp" > README.md
npm init -y

# 首次提交
git add .
git commit -m "chore: initial commit"

# 推送到 GitHub
git remote add origin https://github.com/user/webapp.git
git push -u origin main
```

#### 第2-3天：功能开发

```bash
# 功能1：认证
git checkout -b feature/authentication
# 开发...
git commit -m "feat(auth): add user login"
git push origin feature/authentication

# 功能2：数据展示
git checkout main
git checkout -b feature/data-display
# 开发...
git commit -m "feat(data): add dashboard"
git push origin feature/data-display
```

#### 第4天：Code Review

```bash
# 创建 PR
# 等待审查
# 根据反馈修改
# 合并到 main
```

#### 第5天：测试和发布

```bash
# 创建 release 分支
git checkout -b release/v1.0.0

# 测试
npm test

# 发布
git checkout main
git merge release/v1.0.0
git tag -a v1.0.0 -m "Release 1.0.0"
git push origin main --tags
```

---

## 故障排除

### 常见问题

#### 1. 推送被拒绝

```bash
# 问题
git push
To https://github.com/user/repo.git
 ! [rejected]        main -> main (fetch first)

# 解决
git pull --rebase
git push
```

#### 2. 找不到远程分支

```bash
# 问题
git checkout -b feature/new origin/feature/new
fatal: 'origin/feature/new' does not exist

# 解决
git fetch origin
git branch -a  # 查看所有分支
```

#### 3. 提交信息错误

```bash
# 问题
git commit -m "typo in file"

# 解决
git commit --amend -m "fix: correct typo in user profile"
```

#### 4. 误删分支

```bash
# 问题
git branch -d feature/important
error: The branch 'feature/important' is not fully merged.

# 解决
git reflog  # 找到分支的最后一次提交
git checkout -b feature/important <commit-hash>
```

---

## 最佳实践总结

### 日常开发

1. **分支管理**
   - 每个功能一个分支
   - 定期同步主分支
   - 及时删除无用分支

2. **提交规范**
   - 清晰的提交信息
   - 频繁提交小改动
   - 遵循约定式提交

3. **代码审查**
   - 所有代码经过审查
   - 建设性的反馈
   - 及时响应评论

### 团队协作

1. **沟通**
   - 提前沟通重大变更
   - 使用 PR 评论讨论
   - 及时通知冲突

2. **测试**
   - 提交前本地测试
   - CI/CD 自动化测试
   - 集成测试验证

3. **文档**
   - 更新 README
   - 维护 CHANGELOG
   - 添加代码注释

### 发布流程

1. **准备**
   - 完整的测试
   - 更新文档
   - 创建 release 分支

2. **发布**
   - 打标签
   - 创建 GitHub Release
   - 通知用户

3. **验证**
   - 监控生产环境
   - 准备回滚计划
   - 收集用户反馈

---

## 学习检查清单

### 基础技能

- [ ] 能够创建和切换分支
- [ ] 能够提交和推送代码
- [ ] 能够解决简单冲突
- [ ] 能够查看历史和差异

### 进阶技能

- [ ] 能够使用 rebase 整理历史
- [ ] 能够使用 cherry-pick 挑选提交
- [ ] 能够使用 bisect 查找问题
- [ ] 能够编写有效的 PR 描述

### 高级技能

- [ ] 能够处理复杂冲突
- [ ] 能够执行紧急回滚
- [ ] 能够管理版本发布
- [ ] 能够指导团队成员

---

## 结语

这些实战案例涵盖了 Git 使用的主要场景。通过反复练习，你将能够：

- 🎯 熟练处理日常工作流
- 🔧 解决常见问题
- 👥 高效团队协作
- 🚀 自信地发布版本

记住：熟能生巧，持续练习是掌握 Git 的关键！

---

**📚 回到开始：[README.md](./README.md)**
